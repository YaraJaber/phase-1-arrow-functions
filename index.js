const divide = function(x, y) {
    return x / y;
  };
  
  const result = divide(2000, 100);
  console.log(result);
  
  const square = (x) => x * x;
  
  console.log(square(4)); 
  
  const add = (x, y) => x + y;
  
  console.log(add(4, 3));